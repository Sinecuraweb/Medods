<%= form_with(model: board, local: true) do |form| %>
<% if board.errors.any? %>
<div id="error_explanation">
  <h2><%= pluralize(board.errors.count, "error") %> prohibited this board from being saved:</h2>

  <ul>
    <% board.errors.full_messages.each do |message| %>
    <li><%= message %></li>
    <% end %>
  </ul>
</div>
<% end %>


<%= form.label :Название %>
<%= form.text_field :title, {:class => 'form-control'} %>



<%= form.label :Описание %>
<%= form.text_area :body, {:class => 'form-control' , :rows => '15'}  %>



<%= form.label :author %>
<%= form.text_field :author, {:class => 'form-control'}  %>

<!-- start mini baj(tag) -->
<%#= form.select :tag, options_for_select(@tags), :include_blank => true %>
<%# collection_select( :tag, :tag_id, Board.all, :id, :name, {}) %>


<!-- end tags -->

<!-- <div class="input-group mb-3 mt-3">
  <div class="input-group-prepend">
    <label class="input-group-text" for="inputGroupSelect01">Options</label>
  </div>
  <select class="custom-select" id="inputGroupSelect01">
    <option selected>Choose...</option>
    <option value="1">One</option>
    <option value="2">Two</option>
    <option value="3">Three</option>
  </select>
</div> -->

<!--       <div class="custom-control custom-checkbox mr-sm-2">
        <input type="checkbox" class="custom-control-input" id="customControlAutosizing">
        <label class="custom-control-label" for="customControlAutosizing">Remember my preference</label>
      </div> -->

      <div class="class="mt-3 mb-3">
       

        <%#= @users.each do |user| ?!?!? %> 
        <%#= Tag.all.each do |tag| %>
        <%#= check_box_tag 'tag', tag.id, :class => 'custom-control-input' %>  
        
        <!-- <label class="custom-control-label" > -->

          <%#= tag.name %>
          
          <!-- </label> -->
          <%# end %>
          


          <%#= check_box_tag '11', tag.name, :class => 'custom-control-input' %>  
          
          
          <%#= label_tag(:tag_id, "alb") %>


        </div>


        


        <%# tag_cloud(Tag.counts, %w{tag1 tag2 tag3 tag4 tag5 }) do |tag, css_class| %>
        

        <%#= check_box_tag '11', tag.name, :class => 'custom-control-input' %>  

        <%#= row tag.name, tag_path(tag.name), :class => "btn btn-info btn-sm" %>

        <%# end %>







        <div class="form-group mt-3">
          <%= form.label :'Все Теги' %> 
          <span class="ml-4 text-muted small">
            ( Перечислите через запятую <strong class="pt-4">"  ,  "  </strong> все новые теги, или отредактируйте существующие )
          </span>
          <%= form.text_field :all_tags, class: 'form-control' %>
        </div>

        <!-- start pic -->
        <div class="input-group mb-3 mt-4">
          <div class="input-group-prepend">
            <span class="input-group-text" id="inputGroupFileAddon01">Выберите фото</span>
          </div>
          <div class="custom-file">
            <%= form.file_field :picture, :class => 'custom-file-input' %>
            <label class="custom-file-label" for="inputGroupFile01">Загрузить</label>
          </div>
        </div>
        <!-- end pic -->


        <%= form.submit("Применить изменения", {:class => 'btn btn-success mt-4'}) %>

        <% end %>
